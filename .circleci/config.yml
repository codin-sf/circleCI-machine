version: 2
jobs:
  build:
    machine:
      enabled: true
      image: circleci/classic:latest
    
    steps:
      - checkout

      - run:
          echo "hello world"

      
workflows:
  version: 2
  build-deploy:
    jobs:
      - build
